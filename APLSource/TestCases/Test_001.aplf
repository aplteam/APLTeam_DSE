 R←Test_001(stopFlag batchFlag);⎕TRAP;list
⍝ Check the function key definition and load best version
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 ##.aplteam.Check_FunctionKeyDefinition

 R←T._OK
⍝Done
